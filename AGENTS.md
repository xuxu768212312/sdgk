# 山东高考知识库 - Schema & 决策框架

> 本文件定义知识库的组织约定、操作流程、以及**国内顶尖、国际专业**的志愿填报决策算法框架。
> LLM 在每次操作前**必须先读本文件**，所有策略输出必须遵循此框架。
> 版本：v3.7（2026-06-24 院校地区索引加固版）
> - v1.0：初版 schema
> - v2.0：引入精算/量化金融/运筹学/博弈论/ML 级算法
> - v3.0：System2 明确化（Kahneman 双系统严格映射）+ 多 Agent 协作框架（8 Agent 辩论式制衡）
> - v3.1：强化官方数据源约束：山东省教育招生考试院（sdzk.cn）为唯一主依据，非官方数据不得直接进入正式决策
> - v3.2：引入选科 SQLite 确定性索引、evidence_id 追溯、96 志愿选科硬闸门
> - v3.3：引入互联网数据准入分级、策略优化器硬约束、候选志愿组合风险优化
> - v3.4：策略优化器升级为失败关闭：异常概率、缺效用/来源、重复志愿、保守滑档超限均阻断
> - v3.5：引入低分高价值捕获模型（Value Capture）与 Excel + Markdown 图文双交付标准
> - v3.6：引入 Rush Guard 受控冲刺策略，允许高价值深度冲“搏一搏”，但必须满足概率下限、价值证据、数量上限和保底闸门
> - v3.7：引入院校地区 SQLite 索引，省份查询禁止学校名 contains，城市/多校区不确定必须 REVIEW

---

## 第零章 · 系统级铁律（不可违反）

> 以下 23 条铁律具有最高优先级，任何操作不得违反。冲突时以此为准。

| # | 铁律 | 违反后果 |
|---|------|---------|
| Z1 | **raw 不可变** | 原始资料永不修改，加工只在 processed/wiki |
| Z2 | **位次优先于分数** | 跨年比较只用位次（赋分制使分数跨年不可比） |
| Z3 | **本科专科分开** | 数据池、风险判断、计划来源全部独立 |
| Z4 | **志愿次数对应** | 第 2/3 次志愿必须用当次剩余计划，不可用 round1 数据 |
| Z5 | **考试院唯一主源** | 山东省教育招生考试院（sdzk.cn）发布为唯一正式依据；非官方数据只能作线索，不能作正式决策数值 |
| Z6 | **数据可追溯** | wiki/processed/方案中所有决策数值必须回溯到 raw/ 官方原文或由官方原文脚本生成的 processed/ |
| Z7 | **链接有效** | `[[xxx]]` 必须指向真实存在页面 |
| Z8 | **日志可追溯** | 每次重要操作追加 `log.md` |
| Z9 | **学生信息隔离** | students/ 不入库 raw/processed/wiki，不参与 lint |
| Z10 | **决策可解释** | 每个志愿必须能说出"为什么"（位次依据 + 风险依据 + 偏好依据） |
| Z11 | **风险可量化** | 每个志愿必须有录取概率估计，整体必须有滑档概率估计 |
| Z12 | **反偏差审查** | 正式提交前必须通过 8 类行为经济学偏差清单核查 |
| Z13 | **System2 强制激活** | 触发 8 个条件之一即必须 W.A.I.T.E.R 深度分析，禁止 System1 直出方案 |
| Z14 | **多 Agent 制衡** | 高风险/临界/特殊招生方案必须经 8 Agent 评审，红军否决必须重做 |
| Z15 | **红蓝对抗** | 方案必须有红军 A7 挑刺记录，致命漏洞必须解决才能提交 |
| Z16 | **共识阈值** | 多 Agent 共识度 < 0.75 或偏差指数 > 0.25 必须重做 |
| Z17 | **选科硬闸门** | 选科能否报考必须经 `scripts/check_subject_eligibility.py` 或 `scripts/audit_volunteer_subjects.py` 返回证据；任何 `BLOCK`/`REVIEW` 不得作为正式方案交付 |
| Z18 | **互联网数据准入** | 互联网资料必须先经 `scripts/internet_source_policy.py` 定级；非 S/A/B 级不得进入正式策略算法，第三方数据只能作线索 |
| Z19 | **算法失败关闭** | 策略算法遇到缺字段、异常数值、重复志愿、来源不可追溯或保守滑档超限时必须阻断，不得自动补默认值或夹紧异常值后交付 |
| Z20 | **价值捕获不越过安全闸门** | “低分上好大学/好专业”只能在选科 PASS、来源 S/A/B、保底充足、滑档可控后排序；不得用价值机会覆盖硬风险 |
| Z21 | **双件图文交付** | 正式志愿方案必须同时输出 Excel 志愿表和 Markdown 详细说明报告；报告必须含图表/结构化可视化、风险结论、证据索引 |
| Z22 | **冲刺有界可搏** | 冲刺志愿可以搏一搏，但必须由 `strategy_optimizer_v4_rush_guard_fail_closed` 控制概率下限、深度冲价值证据、深度冲数量上限、保/垫和滑档闸门；不得口头放宽 |
| Z23 | **地区查询走索引** | “山东/江苏/苏州/青岛”等地区偏好必须经 `scripts/check_school_region.py` 或等价索引结果；省份用 `province` 字段，禁止 `school_name contains 山东`；城市不确定或多校区必须 `REVIEW` |

---

## 第一章 · 架构概述

### 四层结构 + 学生工作区

| 层 | 路径 | 谁写 | 性质 |
|---|---|---|---|
| 1. 原始资料 | `raw/` | 人工 | 不可变，只读 |
| 2. 标准数据 | `processed/` | 脚本生成 | 可重复生成，CSV+JSON 双格式 |
| 3. Wiki 知识 | `wiki/` | LLM 维护 | 结构化页面，交叉引用 |
| 4. 处理脚本 | `scripts/` | 人工/LLM | 从 raw 生成 processed |
| 5. 算法引擎 | `algorithms/` | LLM 维护 | 量化决策算法实现（Python） |
| 学生工作区 | `students/` | 人工/LLM | 具体学生咨询材料，**不入库** |

### 目录结构

```
山东高考知识库/
├── AGENTS.md              # 本文件（schema + 决策框架）
├── raw/                   # 原始资料（按年份分目录，只读）
├── processed/             # 标准数据（CSV+JSON）
├── wiki/                  # LLM 维护的知识页面
├── scripts/               # 数据处理脚本
├── algorithms/            # 【新增】量化决策算法引擎
│   ├── position_model.py          # 位次映射模型
│   ├── probability_model.py       # 录取概率估计（贝叶斯+蒙特卡洛）
│   ├── gradient_optimizer.py      # 96志愿梯度优化器
│   ├── kelly_allocator.py         # 凯利公式志愿分配
│   ├── risk_engine.py             # 风险量化引擎
│   ├── bayesian_updater.py        # 动态贝叶斯更新
│   ├── monte_carlo_simulator.py   # 蒙特卡洛模拟器
│   ├── game_theory_analyzer.py    # 博弈论分析（纳什均衡）
│   ├── utility_function.py        # 个性化效用函数
│   ├── bias_checker.py            # 行为经济学偏差核查
│   ├── portfolio_optimizer.py     # 投资组合理论优化
│   └── strategy_optimizer.py      # 失败关闭策略优化器 + 低分高价值捕获排序 + 受控冲刺
└── students/              # 学生工作区（不入库）
```

---

## 第二章 · 数据治理

### 2.1 数据流程

```
raw/（原始 Excel/PDF）
   ↓ scripts/_process_*.py（幂等）
processed/（标准 CSV+JSON）
   ↓ algorithms/*.py（量化引擎）
   ↓ LLM 综合 + 交叉验证
wiki/（结构化知识）
   ↓ + 学生数据
students/<姓名>/志愿方案/（96志愿方案）
```

### 2.2 processed/ 数据集

| 数据集 | 位置 | 字段核心 | 量级 |
|---|---|---|---|
| 一分一段表 | `processed/一分一段表/` | year, score, total_cumulative, 6 科本段/累计 | 约 2700 条 |
| 投档表 | `processed/投档表/` | year, round, major_code/name, school_code/name, plan_count, min_rank | 约 15.1 万条 |
| 分数线 | `processed/分数线/` | 普通类/艺术类/体育类各线 | 2020-2025 |
| 志愿计划 | `processed/志愿计划/` | year, batch, category, school/major, subject_requirement, plan_count | 约 1.1 万条 |
| 选科要求 | `processed/选科要求/` | school/major, subject_requirement, version(2024/2027), evidence_id | 全省覆盖（SQLite 索引 180788 条） |
| 院校地区 | `processed/院校地区/` | school_name, province, city, city_status, evidence_id | 院校覆盖（SQLite 索引 2534 所） |

### 2.3 数据质量分级

| 等级 | 标准 | 用途 |
|------|------|------|
| S 级 | sdzk.cn 官方原文/附件 + 脚本幂等生成 + 行数/字段/抽样回查校验 | 正式决策主依据 |
| A 级 | sdzk.cn 官方原文/附件 + 脚本生成 + 元数据记录完整 | 正式决策主依据 |
| B 级 | sdzk.cn 官方页面截图/HTML/图片 + 人工或 OCR 提取 + 双人/双模型核对 | 临时决策依据，必须标注提取方式 |
| C 级 | 高校招生章程/教育部阳光高考等相关官方渠道 | 合规辅助依据，不替代山东考试院计划/投档数据 |
| D 级 | 第三方网站、媒体、论坛、商业软件、非官方整理表 | 仅作线索，禁止作为正式方案数值依据 |

### 2.4 官方数据源约束（最高优先级）

1. **唯一主源**：山东省教育招生考试院（`sdzk.cn`）是山东高考政策、分数线、一分一段表、招生计划、投档情况、缺额计划、填报时间、录取结果的唯一正式主源。
2. **正式方案用数限制**：学生正式志愿方案中的计划数、位次、分数线、选科要求、批次安排、填报时间，必须来自 S/A/B 级数据；D 级数据不得进入正式方案的数值依据。
3. **冲突处理**：`sdzk.cn` 与任何第三方或历史整理数据冲突时，以 `sdzk.cn` 最新发布为准；若新旧官方文件冲突，以后发布文件为准，并在 wiki/log.md 记录。
4. **非官方数据入库隔离**：确需保存第三方线索时，只能放入 `raw/_common/待核验/` 或 wiki 中标注“待官方核验”，不得写入 `processed/` 正式数据集；已经存在的非官方补充数据必须标明质量等级 D 和使用限制。
5. **元数据必填**：新增或更新 `processed/` 数据时，`_meta.json` 必须记录 source_file、source_url、publisher、download_date/extract_date、quality_level、verification_status。
6. **查询输出标注**：回答涉及数值时，必须标注数据路径和质量等级；若不是 S/A/B 级，必须明确“仅供线索，不作正式填报依据”。
7. **2026 当年数据**：凡涉及 2026 当年计划、缺额、填报时间、投档结果，必须优先检查 `raw/2026/` 中是否已有 sdzk 官方文件；没有则标记“待考试院发布”，不得用 2025 数据冒充 2026 数据。
8. **高校章程边界**：高校招生章程用于体检、单科、外语、校区、学费、培养模式、转专业等合规核查；但山东省内招生计划与投档录取数据仍以山东省教育招生考试院发布为准。

### 2.5 选科防幻觉索引与硬闸门

> 选科可报性属于高考高风险判定，禁止 LLM 直接凭记忆、上下文、大表扫读或向量召回结果判断“能不能报”。

1. **唯一机器判定入口**：
   - 单条院校专业查询：`python scripts/check_subject_eligibility.py ... --json`
   - 96 志愿方案审核：`python scripts/audit_volunteer_subjects.py ... --out <报告路径>.json`
2. **索引来源**：`processed/选科要求/subject_index.sqlite` 只能由 `scripts/build_subject_index.py --rebuild` 从 4 个官方派生 JSON 构建，不得人工编辑 SQLite。
3. **年份映射**：
   - 2025/2026 默认使用 `2024版`
   - 2027 及以后默认使用 `2027版`
   - 其他年份必须 `REVIEW`，不得自动套用
4. **匹配优先级**：
   - 优先 `edition + level + school_code + major_code`
   - 名称匹配仅在唯一命中时作为回退
   - 无命中、多命中、字段缺失、年份无法映射均为 `REVIEW`
5. **状态语义**：
   - `PASS`：可报，必须保留 `evidence_id` 和 `source_file`
   - `BLOCK`：不可报，禁止进入正式方案
   - `REVIEW`：机器无法确定，必须人工回查山东省教育招生考试院官方原文或当年招生计划，禁止口头解释为“应该可以报”
6. **正式方案字段**：最终志愿表/报告必须包含 `subject_check_status`、`evidence_id`、`source_file`；任一志愿为 `BLOCK` 或 `REVIEW`，不得交付为正式方案。
7. **审计要求**：大批量更新选科数据后必须运行 `scripts/build_subject_index.py --rebuild`、`scripts/test_subject_index.py` 和 `scripts/audit_data_accuracy.py`。
8. **候选池前置过滤**：生成候选志愿池时必须先查 SQLite 索引，只允许 `PASS` 候选进入概率、效用、价值捕获计算；`BLOCK` 直接剔除，`REVIEW` 进入人工复核清单，不得混入正式候选池。
9. **证据粒度**：每条候选必须保存 `match_type`、`reason_code`、`subject_requirement_raw`、`evidence_id`、`source_file`；最终 Excel 和 Markdown 报告均必须能反查到这些字段。
10. **代码/名称错位处理**：若投档表院校代码与选科索引 5 位代码体系不一致，只允许在 `school_name + major_name` 唯一命中时回退；名称含“师范类/实验班/中外合作/校区”等后缀不一致时默认 `REVIEW`，不得模糊放行。

### 2.5bis 院校地区索引与地域偏好硬闸门

> 地区偏好会直接影响候选池，不得用院校名称关键词代替真实地区判断。

1. **唯一机器判定入口**：
   - 构建索引：`python scripts/build_school_region_index.py --rebuild`
   - 单校查询：`python scripts/check_school_region.py --regions 山东,苏州 --school-name 青岛大学 --json`
2. **索引来源**：`processed/院校地区/school_region_index.sqlite` 由 `processed/选科要求/*.json` 的 `province` 字段派生，覆盖 2534 所唯一院校；不得人工编辑 SQLite。
3. **省份规则**：
   - “山东的学校”必须匹配索引中的 `province=山东`
   - 禁止使用 `school_name contains 山东`，否则会漏掉青岛大学、济南大学、烟台大学等院校
   - 省份匹配输出 `PROVINCE_MATCH`、`province_exact` 和 `evidence_id`
4. **城市规则**：
   - 城市只在院校名称显式包含城市、县级别名可审计映射、或 reviewed override 时返回 `MATCH`
   - 多校区或城市不确定必须 `REVIEW`，例如山东大学按“青岛”查询不得直接放行，必须结合具体校区/专业原文复核
   - 城市查询不能扩大为整省查询；“苏州”不等于“江苏”
5. **候选池前置过滤**：
   - 地区偏好为硬约束时，只允许 `MATCH` 候选进入正式候选池
   - 地区偏好为软偏好时，`MATCH` 可提高 `location_fit`，`NO_MATCH` 降权，`REVIEW` 进入人工复核清单
6. **正式方案字段**：最终 Excel/Markdown 必须保留 `region_check_status`、`region_reason_code`、`region_match_type`、`region_evidence_id`、`province`、`city`。
7. **不确定性处理**：地区 `REVIEW` 不得口头解释为“应该算目标地区”；必须回查招生章程、校区说明或当年招生计划原文。

### 2.6 互联网数据准入与隔离

> 互联网数据只能在“来源准入 → 官方回查 → raw 快照/附件 → processed 生成 → 策略算法”的链路中使用，禁止直接把网页内容喂给最终方案。

1. **准入入口**：任何 URL 进入咨询、知识库或算法前，先运行 `python scripts/internet_source_policy.py --url <URL>`。
2. **主源判定**：`sdzk.cn`/`wsbm.sdzk.cn` 是山东高考正式主源；可作为 S 级上限，但仍需保存官方原文或附件到 `raw/` 后再生成 `processed/`。
3. **辅助官方源**：教育部、阳光高考、山东省教育厅、高校官网只能作 C 级合规辅助，用于招生章程、体检、单科、外语、校区、学费等，不替代山东考试院计划/位次/投档。
4. **第三方隔离**：媒体、论坛、商业软件、公众号、问答平台、非官方整理表默认为 D 级，只能放入待核验线索，不得写入 `processed/`，不得进入正式策略算法。
5. **算法输入要求**：`algorithms/strategy_optimizer.py` 只接受 `source_quality` 为 S/A/B 且 `subject_check_status=PASS` 且含 `evidence_id` 的候选志愿；否则自动阻断。
6. **当前性要求**：涉及 2026 当年计划、缺额、志愿填报入口状态、分数线、一分一段、投档结果时，必须在操作当日回查山东省教育招生考试院最新页面或本地 raw 快照。

---

## 第三章 · 操作流程

### 3.1 Ingest（入库新资料）

1. 资料放入 `raw/<year>/` 对应目录
2. LLM 执行：
   - 先核验来源是否为 `sdzk.cn` 或高校/教育部官方渠道，记录 source_url、发布机构、发布日期、下载日期
   - 编写或更新 `scripts/_process_*.py`，重跑生成 `processed/`
   - 更新 `processed/<数据集>/_meta.json`，写入 quality_level 与 verification_status
   - 写入或更新 wiki 页面（实体页 + 相关概念/主题页）
   - 更新 `wiki/index.md`
   - 追加 `wiki/log.md`：`## [YYYY-MM-DD] <type> | <标题>`

### 3.2 Query（查询）

1. LLM 读 `wiki/index.md` 定位相关页面
2. 深入阅读具体 wiki 页面
3. 必要时读 `processed/` 标准数据
4. 涉及互联网资料时，先运行 `scripts/internet_source_policy.py` 做来源准入，非 S/A/B 级不得作为正式数值依据
5. 涉及选科能否报考时，必须运行 `scripts/check_subject_eligibility.py`，回答中引用 `status`、`reason_code`、`evidence_id`、`source_file`
6. 综合回答并标注引用（页面路径 + 数据文件路径 + 数据质量等级）
7. 有价值的回答回写到 wiki

### 3.3 Lint（健康检查，每季度或大批量入库后）

- 链接完整性 / 孤立页面 / 数据一致性 / 官方来源追溯 / 非官方数据隔离 / 过时声明 / 矛盾内容 / 缺失概念页 / Frontmatter 完整性
- 生成 `wiki/lint_<date>.md`，追加 `log.md`
- **lint 只扫描 `wiki/`，不扫描 `students/`**

### 3.4 Consult（学生咨询，SOP 化）

```
Phase 1: 信息采集 → 基本信息.md（必填项 + 选填项）
Phase 2: 定位分析 → 位次映射 + 分档判定 + 选科可报范围（必须走 SQLite 选科索引）
Phase 3: 候选池生成 → 先选科 PASS + 地区 MATCH/软偏好标注，再算概率/效用/价值捕获，不允许先猜后审
Phase 4: 策略生成 → System2 六步流程 + 互联网数据准入 + 96志愿梯度优化 + Rush Guard 受控冲刺
Phase 5: 风险评估 → 蒙特卡洛 10000 次模拟 + 8类偏差核查 + 选科硬闸门审核
Phase 6: 方案交付 → Excel 志愿表 + Markdown 图文说明报告（docx 可选）
Phase 7: 追踪复盘 → 录取结果回溯，校准概率模型
```

---

## 第四章 · 山东高考领域知识

### 4.1 考试模式

山东夏季高考 **3+3 模式**：
- 3 科统一高考：语文、数学、外语（各 150 分，原始分）
- 3 科等级考：从物化生政史地 6 科选 3 科（各 100 分，等级赋分）
- 总分 750 分
- 8 等级赋分：A(91-100), B+(81-90), B(71-80), C+(61-70), C(51-60), D+(41-50), D(31-40), E(21-30)

### 4.2 志愿填报

- 志愿单位：**"专业（专业类）+ 学校"**
- 普通类常规批：3 次志愿，每次最多 96 个
- 普通类提前批：分 A 类（非平行）和 B 类（平行，4 个类型）
- 艺术类、体育类：志愿数 2026 起增至 96 个
- 2026 起取消特殊类型批，高校专项并入常规批

### 4.3 分数线

- **一段线**：本科计划 1:1.2
- **二段线**：本专科计划总数 + 生源
- **特殊类型招生控制线**：本科计划 1:0.5
- **3+2 对口贯通资格线**：单独划定

### 4.4 主要实体/概念/主题

详见 `wiki/index.md`。

---

## 第五章 · 顶尖决策框架（核心加固部分）

> 本章引入**精算学、量化金融、运筹学、博弈论、机器学习、行为经济学、贝叶斯统计、风险科学、决策论**九大学科的专业算法，构建国内顶尖的志愿填报决策框架。

### 5.1 核心理念

志愿填报是**高风险、不可逆、信息不完全、零和博弈**的决策问题。必须采用：

1. **System2 慢思考**（Daniel Kahneman）：避免直觉启发式偏差
2. **数据驱动**：所有判断基于 `processed/` 标准数据
3. **概率推断**：用贝叶斯+蒙特卡洛量化不确定性
4. **优化理论**：用凯利公式+投资组合理论做资源分配
5. **风险科学**：用 VaR/CVaR/Tail Risk 量化极端情况
6. **博弈论**：在零和博弈中找纳什均衡与价值洼地
7. **行为经济学**：8 类偏差清单强制核查
8. **个性化效用**：用期望效用理论做个性化匹配
9. **多 Agent 协作**：8 个专业 Agent 辩论式制衡（见 5.14）

**顶尖专家水平** = 数据驱动 + 流程化 + 风险量化 + 个性化匹配 + 反偏差审查 + 多Agent制衡

---

### 5.1bis · System2 思维框架（Kahneman 双系统严格映射）

> **核心文献**：Daniel Kahneman《Thinking, Fast and Slow》(2011)
> **强制要求**：所有志愿填报决策必须显式激活 System2，禁止 System1 直接产出方案。

#### 5.1bis.1 双系统对照

| 维度 | System1（快思考） | System2（慢思考） |
|------|------------------|------------------|
| **触发** | 自动、无意识 | 需主动调用、消耗认知资源 |
| **速度** | 毫秒级 | 秒级到小时级 |
| **方式** | 直觉、启发式、模式匹配 | 演绎、计算、概率推理 |
| **能耗** | 低 | 高（易疲劳） |
| **典型表现** | "这个学校名字响亮，填！" | "查近3年位次、算概率、比效用" |
| **偏差** | 锚定、可得性、代表性、损失厌恶 | 可识别并修正偏差 |
| **适用** | 简单熟悉场景 | 复杂高风险场景 |

志愿填报**必须 System2 主导**，System1 仅用于初步筛选和情感反馈。

#### 5.1bis.2 System2 强制激活触发条件

以下 8 个条件**任一**触发，必须显式激活 System2：

| # | 触发条件 | 检测方式 |
|---|---------|---------|
| T1 | 考生位次在目标院校±20% 范围内 | 位次比检查 |
| T2 | 目标院校近3年位次波动 $\sigma/\mu > 0.15$ | RSI < 0.85 |
| T3 | 涉及中外合作/走读/地方专项等特殊培养模式 | 关键词匹配 |
| T4 | 考生体检受限 | 体检结论非"合格" |
| T5 | 选科组合可报专业覆盖率 < 60% | 选科可报范围计算 |
| T6 | 单志愿录取概率在 30-70%（临界区） | 概率模型输出 |
| T7 | 整体方案滑档概率 > 3% | 蒙特卡洛模拟 |
| T8 | 考生偏好权重冲突（如就业 vs 兴趣，权重相近） | AHP 一致性比率 > 0.1 |

#### 5.1bis.3 System2 思考清单（W.A.I.T.E.R 框架）

每次决策前，LLM 必须逐项回答以下 6 组问题：

| 字母 | 含义 | 思考问题 |
|------|------|---------|
| **W** | Why（动因） | 为什么选这个？是数据驱动还是直觉？ |
| **A** | Alternatives（替代） | 还有哪些替代方案？为什么没选？ |
| **I** | Information（信息） | 信息完整吗？哪些是已知的？哪些是不确定的？ |
| **T** | Trade-offs（权衡） | 在取舍什么？损失了什么？获得了什么？ |
| **E** | Evidence（证据） | 证据有多强？样本量多大？是否可证伪？ |
| **R** | Review（复核） | 反向论证：列出3个不应选的理由 |

**强制输出格式**：

```markdown
### System2 思考记录

**W (Why)**: 选 X 校 Y 专业，因为近3年位次 110k~120k，考生位次 119k，位次比 0.92，概率 65%。非直觉。
**A (Alternatives)**: 备选 Z 校同专业（位次 125k，概率 85%），但效用分低 15%。
**I (Information)**: 已知 2023-2025 投档位次（3年），未知 2026 招生计划（预计6月底发布）。
**T (Trade-offs)**: 牺牲概率换取效用，风险可控（在稳档）。
**E (Evidence)**: 历史样本 n=3，RSI=0.88（稳定），证据强度中。
**R (Review)**: 反向理由：①该校该专业 2024 年扩招 20%，2026 可能缩招；②学费偏高；③城市非首选。
**结论**: 可填，放在稳档第 12 位。
```

#### 5.1bis.4 System1 vs System2 协作原则

1. **System1 先行**：快速筛选出候选池（100-200 个志愿）
2. **System2 主导**：量化分析、排序、风险评估
3. **System1 复核**：最后让考生"感觉"一下方案是否舒服（情感反馈）
4. **冲突仲裁**：若 System1 反感而 System2 推荐，强制 W.A.I.T.E.R 清单
5. **疲劳管理**：System2 高能耗，每 30 分钟休息，避免决策疲劳导致 System1 接管

#### 5.1bis.5 System2 禁用场景

以下场景**禁止使用 System1**，必须 System2：

- 冲稳保垫梯度比例设计
- 录取概率估计
- 风险量化（滑档/退档）
- 多目标权衡（学校/专业/城市）
- 特殊类型招生（公费生/专项/强基）
- 体检受限专业判断
- 中外合作办学费用决策
- 96 志愿最终排序

#### 5.1bis.6 LLM 自检协议

LLM 在输出方案前，必须内部完成：

```
[ ] 我是否激活了 System2？
[ ] 是否回答了 W.A.I.T.E.R 6 组问题？
[ ] 是否触发了 8 个激活条件之一？若触发，是否做了深度分析？
[ ] 是否通过了 8 类偏差核查？
[ ] 方案是否有数据依据（非"我觉得"）？
```

任一未通过，不得输出最终方案。

### 5.2 System2 六步决策流程

```
Step 1: 定位（Positioning）
  ↓ 位次法 + 同位次映射 + 分档判定
Step 2: 筛选（Filtering）
  ↓ 选科要求 + 批次 + 类别 + 体检 + 偏好 多重过滤
Step 3: 排序（Ranking）
  ↓ 量化打分模型 + 效用函数排序
Step 4: 风险评估（Risk Assessment）
  ↓ 蒙特卡洛模拟 + VaR/CVaR + 极端情况分析
Step 5: 优化（Optimization）
  ↓ 凯利公式分配 + 投资组合优化 + MECE 分类
Step 6: 验证（Validation）
     防错清单 + 8类偏差核查 + 模拟投档 + 纳什均衡检验
```

---

### 5.3 位次映射模型（Position Mapping Model）

> **核心思想**：山东赋分制使分数跨年不可比，但位次是可比的。建立多年度位次映射表，用历史数据预测今年。

#### 5.3.1 同位次映射

对考生位次 $r_{考生}$，在历史年 $y$ 的一分一段表中查找对应分数 $S_y(r)$：

$$S_y(r) = \max\{s : \text{Cumulative}(y, s) \leq r\}$$

#### 5.3.2 位次稳定性指数（RSI, Rank Stability Index）

$$RSI_{学校/专业} = 1 - \frac{\sigma_{位次}}{\mu_{位次}}$$

- $RSI > 0.9$：极稳定（如老牌师范、医学），可直接用历史位次
- $0.7 < RSI \leq 0.9$：稳定，需轻微调整
- $RSI \leq 0.7$：不稳定（如新设专业、扩招/停招），需贝叶斯修正

#### 5.3.3 同位次等效分（Equivalent Score）

$$S^* = \frac{1}{N}\sum_{y=2021}^{2025} S_y(r_{考生}) \cdot w_y, \quad w_y = \frac{y - 2020}{\sum(y-2020)}$$

加权平均（近期年份权重更大），输出"等效分"用于直观沟通。

---

### 5.4 录取概率估计模型（Admission Probability Estimation）

> **核心思想**：录取概率不是"有/没有"，而是一个**连续概率分布**。结合历史数据 + 当年变化做贝叶斯推断。

#### 5.4.1 基础概率函数（Logistic 回归）

$$P_{基础} = \sigma\left(\beta_0 + \beta_1 \cdot \ln\left(\frac{r_{考生}}{r_{目标}}\right) + \beta_2 \cdot \text{RSI}\right)$$

参数校准：
- $\beta_1 \approx -3.5$（位次比每降低 10%，概率上升约 30%）
- $\beta_2 \approx 0.4$（RSI 越高，预测越可靠）
- 用 2021-2025 历史数据做极大似然估计（MLE）

简化版经验公式（用于无校准数据时）：

$$P = \sigma\left(8 \cdot \left(1 - \frac{r_{考生}}{r_{目标}}\right)\right)$$

| 位次比 $r_{考生}/r_{目标}$ | 录取概率 | 梯度 |
|---------------------------|---------|------|
| ≤ 0.80 | 95%+ | 垫 |
| 0.80-0.90 | 85-95% | 保 |
| 0.90-1.00 | 60-80% | 稳 |
| 1.00-1.10 | 30-60% | 冲（稳上） |
| 1.10-1.20 | 10-30% | 冲 |
| ≥ 1.20 | <10% | 弃 |

#### 5.4.2 贝叶斯动态更新（Bayesian Updating）

当出现新证据 $E$（扩招/缩招/政策变化/同分段考生数变化），用贝叶斯公式更新：

$$P_{后验} = \frac{P(E|录取) \cdot P_{先验}}{P(E)}$$

证据权重表：

| 证据类型 | 更新方向 | 修正系数 |
|---------|---------|---------|
| 扩招 > 20% | ↑ | $\times 1.15$ |
| 缩招 > 20% | ↓ | $\times 0.85$ |
| 新增专业（无历史） | ↓ | $\times 0.70$（信息不足） |
| 选科要求收紧 | ↑/↓ | 视考生选科而定 |
| 公费生计划增加 | ↑（对该方向） | $\times 1.10$ |
| 同分段考生数 +10% | ↓ | $\times 0.92$ |

#### 5.4.3 蒙特卡洛模拟（Monte Carlo Simulation）

对每个志愿 $i$，录取概率 $P_i$ 本身是一个分布（不确定性）。用 Beta 分布建模：

$$P_i \sim \text{Beta}(\alpha_i, \beta_i), \quad \alpha_i = n_{历史} \cdot \bar{P}, \quad \beta_i = n_{历史} \cdot (1-\bar{P})$$

模拟流程：
1. 对每个志愿 $i$，从 $\text{Beta}(\alpha_i, \beta_i)$ 采样 $P_i$
2. 模拟投档：以概率 $P_i$ 决定是否录取
3. 重复 10000 次，统计：
   - **滑档概率**：96 个志愿全部未录取的比例
   - **录取分布**：录取到每个志愿的频率
   - **Tail Risk**：5% 最差情况的录取结果

输出：滑档概率 $P_{滑档}$ + 录取志愿分布 + CVaR（条件风险价值）

---

### 5.5 96 志愿梯度优化器（Gradient Optimizer）

#### 5.5.1 凯利公式分配（Kelly Criterion）

> **类比**：志愿分配 = 赌注分配。凯利公式在不确定下最大化长期增长率。

定义"志愿价值" $b_i$ = 录取后效用 - 未录取效用（期望增益）。
录取概率 $p_i$，未录取概率 $q_i = 1-p_i$。

凯利比例：

$$f_i^* = \frac{p_i \cdot b_i - q_i}{b_i} = p_i - \frac{q_i}{b_i}$$

约束：$\sum f_i = 96$（总志愿数）。

**简化应用**（经验版）：
- 冲（$p=0.2-0.4$, $b=2-3$）：$f^* \approx 0.25 \times 96 = 24$
- 稳（$p=0.6-0.8$, $b=1$）：$f^* \approx 0.42 \times 96 = 40$
- 保（$p=0.9+, b=0.5$）：$f^* \approx 0.27 \times 96 = 26$
- 垫（$p=0.99+, b=0.1$）：$f^* \approx 0.06 \times 96 = 6$

**理论最优比例 24:40:26:6**，整体滑档概率 < 1%。

#### 5.5.2 投资组合理论优化（Markowitz Mean-Variance）

把 96 志愿看作一个投资组合：
- **收益**：录取到心仪志愿的期望效用
- **风险**：滑档概率 + 录取到不心仪志愿的概率

目标函数：

$$\max_{\{x_i\}} \quad \mu^T x - \lambda \cdot x^T \Sigma x$$

$$\text{s.t.} \quad \sum x_i = 96, \quad x_i \in \{0, 1\}, \quad x_{\text{垫}} \geq 6$$

其中 $\mu_i$ = 志愿 $i$ 的期望效用，$\Sigma$ = 志愿间协方差矩阵（用历史投档位次相关性估计）。

#### 5.5.3 MECE 分类（Mutually Exclusive, Collectively Exhaustive）

96 志愿必须 MECE 分类，避免重复或遗漏：

```
按梯度：冲 / 稳 / 保 / 垫（互斥）
按地域：本省 / 江苏 / 其他（互斥）
按专业类：师范 / 文法 / 经管 / 医药 / 其他（互斥）
按培养模式：普通 / 中外合作 / 走读 / 地方专项（互斥）
```

---

### 5.6 风险量化引擎（Risk Engine）

#### 5.6.1 风险类型

| 风险类型 | 定义 | 量化指标 |
|---------|------|---------|
| **滑档风险** | 96 志愿全部未投档 | $P_{滑档} = \prod(1-P_i)$ |
| **退档风险** | 投档后因体检/单科等被退 | 按体检受限项加权 |
| **调剂风险** | 录取到不心仪专业 | 山东"专业+学校"模式下调剂风险低 |
| **专业错配风险** | 录取但专业方向不符 | 效用函数评估 |
| **极端风险** | 招生计划突变/政策变化 | 蒙特卡洛 Tail Risk |

#### 5.6.2 风险度量指标

**Value at Risk (VaR)**：95% 置信下，最差录取志愿的位次
**Conditional VaR (CVaR)**：最差 5% 情况下的平均录取位次
**Tail Risk**：极端情况下的录取结果分布

```python
# 伪代码
var_95 = np.percentile(simulation_results, 5)
cvar = simulation_results[simulation_results <= var_95].mean()
```

#### 5.6.3 风险等级

| 等级 | 滑档概率 | 垫底志愿数 | Tail Risk | 建议 |
|------|---------|-----------|-----------|------|
| 极低 | < 1% | ≥ 6 | 可控 | 直接提交 |
| 低 | 1-5% | 4-6 | 可接受 | 提交 |
| 中 | 5-15% | 2-4 | 较高 | 增加垫底 |
| 高 | > 15% | < 2 | 极高 | 重新设计梯度 |

#### 5.6.4 退档风险专项量化

山东"专业+学校"模式下，投档后退档原因：

| 退档原因 | 检测方式 | 权重 |
|---------|---------|------|
| 体检受限 | 体检结论 × 专业限报表 | 0.4 |
| 单科成绩不达标 | 单科成绩 × 专业单科要求 | 0.3 |
| 外语语种限制 | 外语语种 × 专业要求 | 0.15 |
| 性别限制 | 性别 × 专业要求 | 0.10 |
| 年龄/其他 | 特殊要求 | 0.05 |

退档风险指数 $R_{退档} = \sum w_i \cdot \mathbb{1}[\text{受限}]$。

---

### 5.7 个性化效用函数（Utility Function）

> **核心思想**：不同考生有不同的价值体系。用效用函数量化个性化偏好。

#### 5.7.1 多维效用函数

$$U(志愿_i) = \sum_{k=1}^{8} w_k \cdot u_k(志愿_i)$$

| 维度 $k$ | 含义 | 评分方式 |
|---------|------|---------|
| 就业前景 | 毕业后就业率/薪资 | 1-10 分 |
| 考研适配 | 本专业考研方向匹配 | 1-10 分 |
| 考公适配 | 国考/省考岗位匹配 | 1-10 分 |
| 出国适配 | 海外名校认可度 | 1-10 分 |
| 专业兴趣 | 考生兴趣匹配 | 1-10 分 |
| 城市偏好 | 城市资源/离家距离 | 1-10 分 |
| 学费负担 | 家庭经济承受 | 1-10 分 |
| 学校层次 | 985/211/双一流/普本 | 1-10 分 |

权重 $w_k$ 通过咨询采集，$\sum w_k = 1$。

#### 5.7.2 权重采集问卷

向考生提问（李克特 5 级量表）：
1. "毕业后最希望做什么？"（就业/考研/考公/出国）
2. "对专业兴趣有多强？"
3. "对城市有偏好吗？"
4. "家庭经济承受能力？"
5. "能接受的学费上限？"

用 AHP（层次分析法）或直接打分法计算权重。

#### 5.7.3 HARA 效用函数（风险厌恶参数）

$$U(x) = \frac{1-\gamma}{1-\gamma} \cdot (x + x_0)^{1-\gamma}$$

- $\gamma > 1$：风险厌恶（保守型考生，多填保底）
- $\gamma = 1$：风险中性（标准梯度）
- $\gamma < 1$：风险偏好（激进型考生，多填冲刺）

根据考生风险偏好调整梯度比例。

---

### 5.8 博弈论视角（Game Theory）

#### 5.8.1 志愿填报是零和博弈

山东"专业+学校"平行志愿下，每个招生计划只能录取一人，是严格零和博弈。

#### 5.8.2 纳什均衡与价值洼地

**价值洼地**：被多数考生忽略的好学校/好专业（信息不对称导致）。

识别方法：
1. **历史低位次波动**：某校 3 年位次波动大，说明被关注度不稳定
2. **新增专业**：第 1 年招生的专业，信息未扩散
3. **地理位置劣势**：偏远地区的 211/双一流
4. **冷门专业 + 强校**：名校的非热门专业
5. **省外强校省内知名度低**：如某些中西部 985 在山东知名度低

#### 5.8.3 混合策略纳什均衡

考生应在"冲稳保垫"间采取混合策略，而非纯策略（全冲或全保）。凯利公式给出最优混合比例。

#### 5.8.4 博弈论三原则

1. **避免正面对抗**：高分考生扎堆的热门专业，同等分数下避开
2. **寻找弱对手**：选择自己分数占优的院校专业
3. **信号解读**：从招生章程变化、专业调整读出"扩招/缩招"信号

---

### 5.9 分档决策模型（不同分数段策略）

#### 5.9.1 分档策略表

| 分数段 | 位次参考 | 优先级 | 策略类型 | 风险偏好 | 关键考量 |
|--------|---------|--------|---------|---------|---------|
| **顶尖分段**（清北复交） | < 1,000 | 学校 > 专业 > 城市 | 激进型 | $\gamma < 1$ | 冲名校，专业可调剂 |
| **高分段**（985/211） | 1k-15k | 学校 + 专业 > 城市 | 稳妥型 | $\gamma = 1$ | 冲名校+专业方向 |
| **中高分段**（强双一流） | 15k-50k | 专业 + 学校 > 城市 | 稳妥型 | $\gamma = 1$ | 专业与城市平衡 |
| **中分段**（普通本科） | 50k-150k | 专业 > 城市 > 学校 | 稳妥型 | $\gamma = 1$ | 就业/考研导向 |
| **中低分段**（民办本科） | 150k-300k | 专业 + 就业 > 城市 | 保守型 | $\gamma > 1$ | 技术导向，本科优先 |
| **低分段**（专科） | > 300k | 专业 > 就业 > 城市 | 保守型 | $\gamma > 1$ | 专本贯通/技术就业 |

#### 5.9.2 分档决策树

```
有明确专业兴趣？
  ├─ 是 → 专业优先
  │       ├─ 高分段 → 冲名校该专业
  │       └─ 中低分段 → 选该专业强校
  └─ 否 → 分数段判断
           ├─ 顶尖/高分 → 学校优先（进校后分流）
           ├─ 中分 → 城市优先（就业/考研资源）
           └─ 低分 → 专业优先（技术就业）
```

#### 5.9.3 四优先策略（胡向东教授）

1. **地域优先**：职业方向不明确时，城市决定文化底蕴、教学资源、就业机会
2. **职业优先**：职业选择已"有谱"、分数较高有选择余地
3. **专业优先**：专业特长明显，或分数较低学校选择性不多
4. **学校优先**：无明确职业方向、成绩均衡；踩线考生；计划出国深造

#### 5.9.4 张雪峰八连问方法论

就业导向专业选择决策树：

```
学医？ → 学农？ → 当老师？ → 文理兼招？ → 带化学？ → 带物理？ → 三方向 → 带数学？
```

适用：普通家庭、就业导向、无明确兴趣。与胡向阳"四优先"互补。

---

### 5.9bis 低分高价值捕获模型（Value Capture Model）

> 核心目标：在不突破选科、来源、保底和滑档硬闸门的前提下，识别“低分上好大学/好专业”的真实机会，而不是制造“捡漏”幻觉。

#### 5.9bis.1 定义

“低分高价值”不是单纯冲高，也不是用低概率博运气，而是寻找以下不完全定价机会：

| 机会类型 | 判定逻辑 | 风险提示 |
|---|---|---|
| 好大学低分入口 | 院校层次高，但某专业/校区/培养方向近年投档位次低于同层次均值 | 必须核查校区、学费、培养模式、转专业限制 |
| 好专业低分入口 | 专业就业/考研/考公/证照价值高，但院校热度较低或地域关注度低 | 必须核查专业是否真相关，不用名称相似替代 |
| 地域错配机会 | 省外优质院校在山东认知度低，位次被低估 | 必须确认考生能接受城市和距离 |
| 新增/恢复招生机会 | 首年或恢复招生导致历史锚点不足 | 默认提高不确定性，不能作为保底 |
| 冷热门错位机会 | 强校冷门专业、普通校强势专业、师范/语言/财经等细分错配 | 必须结合考生偏好和就业路径 |

#### 5.9bis.2 前置硬条件

以下条件全部满足后，才允许计算价值捕获分：

1. 选科索引返回 `PASS`，并保留 `evidence_id`、`source_file`、`subject_requirement_raw`
2. 投档位次/计划/选科来源质量为 S/A/B
3. 本科/专科、批次、志愿次数、计划来源匹配
4. 候选不在体检、单科、外语语种、性别、学费承受能力的硬限制内
5. 整体方案已有足够 `保/垫`，且保守滑档概率不超限

#### 5.9bis.3 价值捕获评分

候选志愿的价值捕获分 `V`：

```text
V = 0.28 * school_value
  + 0.24 * major_value
  + 0.18 * preference_fit
  + 0.12 * location_fit
  + 0.12 * value_opportunity
  + 0.03 * affordability
  + 0.03 * plan_stability
```

字段含义：

| 字段 | 含义 | 推荐来源 |
|---|---|---|
| `school_value` / `school_tier` | 院校层次与办学价值，985/211/双一流/省重点/公办/民办等 | 官方名单、学校官网、教育部/高校信息 |
| `major_value` | 专业强度、就业/考研/考公/证照价值 | 官方专业目录、招生章程、就业质量报告（辅助） |
| `preference_fit` | 与考生兴趣、职业目标、家庭约束的匹配度 | 偏好问卷、面谈记录 |
| `location_fit` | 城市、距离、区域就业资源匹配 | 考生偏好 + 官方校区信息 |
| `value_opportunity` | 低位次获得高院校/专业价值的性价比空间 | 历史投档位次、同层次对比、计划变化 |
| `affordability` | 学费、住宿、生活成本可承受度 | 招生章程/官方收费标准 |
| `plan_stability` | 计划数量和招生连续性稳定程度 | 当年计划 + 近年计划 |

#### 5.9bis.4 策略总分

`algorithms/strategy_optimizer.py` 使用失败关闭的价值捕获排序：

```text
strategy_score =
    0.38 * utility
  + 0.23 * probability
  + 0.10 * RSI
  + 0.07 * source_quality
  + 0.22 * value_capture_score
  - tail_penalty
  - affordability_penalty
```

解释：

- `utility` 仍代表考生个性化满意度，是主轴
- `probability` 控制录取确定性，不能被价值分覆盖
- `value_capture_score` 用来优先发现“低分高价值”机会
- `tail_penalty` 和 `affordability_penalty` 用来压制高尾部风险和费用风险
- 任一硬闸门不通过时，即使 `value_capture_score=1.0` 也不得进入正式方案

#### 5.9bis.5 输出要求

每条入选志愿必须输出：

- `value_capture_score`
- `value_capture_reasons`（如 `HIGH_SCHOOL_VALUE`、`HIGH_MAJOR_VALUE`、`LOW_RANK_VALUE_GAP`、`HIGH_PREFERENCE_FIT`）
- 低分高价值解释：为什么它比同梯度普通志愿更值得
- 风险反证：为什么它可能不是机会，而是信息陷阱

禁止表述：

- “这个学校低分一定能捡漏”
- “往年低位次所以今年稳”
- “名称看起来相近所以可当作目标专业”
- “选科应该没问题”

---

### 5.9ter 受控冲刺策略（Rush Guard）

> 核心目标：在不牺牲安全底座的前提下，提高“低分上好大学/好专业”的上行空间。冲刺可以搏一搏，但只能让算法搏，不能让口头感觉搏。

#### 5.9ter.1 概率分层

`algorithms/strategy_optimizer.py` v4 将冲刺拆成两层：

| 层级 | 概率区间 | 用途 | 硬要求 |
|---|---|---|---|
| 普通冲 | `probability >= deep_rush_probability` 且 `< 0.60` | 主要冲刺空间 | 必须 S/A/B 来源、选科 PASS、效用完整 |
| 深度冲 | `min_rush_probability <= probability < deep_rush_probability` | 少量高价值搏机会 | 必须 `value_capture_score` 达标，且数量不超过风险档上限 |
| 弃 | `probability < min_rush_probability` | 不进入正式排序 | 除非更新官方证据并重算概率，否则不得人工放行 |

默认分界：

- `conservative`：最低冲刺概率 20%，不允许深度冲
- `standard`：最低冲刺概率 15%，深度冲上限 4 个/96
- `aggressive`：最低冲刺概率 12%，深度冲上限 8 个/96
- `opportunistic`：最低冲刺概率 10%，深度冲上限 10 个/96，仅用于明确接受上行机会且保/垫充足的考生

#### 5.9ter.2 深度冲准入

深度冲必须同时满足：

1. `subject_check_status=PASS` 且有 `evidence_id`
2. `source_quality` 为 S/A/B，且有 `source_file`
3. `value_capture_score >= min_deep_rush_value_capture`
4. 不是保底、不是兜底、不是唯一满意路径
5. 进入组合后保/垫数量仍达标，`conservative_slip_probability` 不超风险档上限

不满足第 3 条时，优化器必须输出 `LOW_PROBABILITY_WITHOUT_VALUE_CAPTURE`；超过数量上限时，必须输出 `DEEP_RUSH_OVER_PROFILE_CAP` 或 `DEEP_RUSH_TOO_MANY`。

#### 5.9ter.3 填报解释要求

正式报告中，所有 `rush_tier=deep_rush` 的志愿必须单独标注：

- 录取概率和所属风险档
- 为什么值得搏：院校价值、专业价值、地域错配、计划变化或偏好匹配
- 为什么可能失败：位次差、计划波动、热度回升、样本不足
- 它不是保底，失败后由哪些稳/保/垫志愿承接

禁止把深度冲描述为“稳一稳”“大概率能上”或“捡漏确定”。深度冲只能提高上行收益，不能承担安全功能。

---

### 5.10 行为经济学偏差核查清单（Bias Checklist）

> **强制要求**：正式提交志愿前，必须通过 8 类偏差核查。

| # | 偏差类型 | 表现 | 核查问题 | 纠正方法 |
|---|---------|------|---------|---------|
| B1 | **损失厌恶**（Loss Aversion） | 过度担心滑档，全填保底 | "你是更怕滑档还是更怕错过冲刺机会？" | 用期望效用计算 |
| B2 | **沉没成本**（Sunk Cost） | 因为研究了某校很久就不愿放弃 | "如果今天才第一次看到这所学校，你还会填吗？" | 重新独立评估 |
| B3 | **锚定效应**（Anchoring） | 被第一个看到的学校/专业影响 | "你的目标院校清单是怎么来的？" | 多源独立参考 |
| B4 | **确认偏差**（Confirmation） | 只找支持自己选择的证据 | "列出3个不应填这所学校的理由" | 强制反向论证 |
| B5 | **过度自信**（Overconfidence） | 高估录取概率 | "如果录取概率只有50%，你还填吗？" | 用历史数据校准 |
| B6 | **从众心理**（Herding） | 跟随同学/亲戚的选择 | "如果没有别人填，你会填吗？" | 独立决策 |
| B7 | **现状偏差**（Status Quo） | 不愿调整初始方案 | "如果重新设计，你会怎么改？" | 重新设计 |
| B8 | **框架效应**（Framing） | 被"冲稳保"标签限制 | "如果只看录取概率和效用，忽略梯度标签？" | 重新排序 |

**偏差指数** = 被触发的偏差数量 / 8。
- 0-2：可提交
- 3-4：需调整
- ≥ 5：重新设计

---

### 5.11 高级决策模型集成

所有模型在 `algorithms/` 下实现，详见 `wiki/topics/topic_高级决策模型.md`：

| 模型 | 用途 | 输入 | 输出 |
|------|------|------|------|
| 位次映射模型 | 跨年位次比较 | 考生位次 + 历史一分一段 | 等效分 + RSI |
| 录取概率模型 | 量化录取概率 | 考生位次 + 目标位次 + RSI | 概率分布 |
| 贝叶斯更新器 | 动态修正概率 | 先验 + 新证据 | 后验概率 |
| 蒙特卡洛模拟器 | 风险分布模拟 | 96志愿概率分布 | 滑档概率 + CVaR |
| 凯利分配器 | 志愿数分配 | 概率 + 效用 | 24:40:26:6 |
| 投资组合优化器 | 志愿组合优化 | 效用 + 协方差 | 最优96志愿 |
| 博弈论分析器 | 价值洼地识别 | 历史位次 + 招生变化 | 价值洼地清单 |
| 效用函数 | 个性化匹配 | 考生偏好 + 志愿特征 | 效用得分 |
| 偏差核查器 | 反偏差审查 | 方案 + 考生回答 | 偏差指数 |
| 风险引擎 | 风险量化 | 方案 + 蒙特卡洛 | VaR/CVaR/滑档 |

---

### 5.12 位次法核心公式（操作级）

#### 5.12.1 位次法

**原理**：山东等级赋分制下，跨年比较用位次不用分数。

**操作**：
1. 查 `processed/一分一段表/<year>.json`，找到考生位次对应分数
2. 查 `processed/投档表/<year>_round1.json`，找到目标专业/学校近 3 年最低投档位次
3. 比较：考生位次 vs 目标位次

**公式**：

$$P_{录取} \approx f\left(\frac{r_{考生}}{r_{目标}}\right)$$

- 比值 < 0.9 → 冲刺候选，必须由概率模型输出；普通冲通常 ≥ 20%，受控深度冲可到 10%-20%，但必须通过 Rush Guard
- 比值 0.9-1.1 → 稳妥（概率 60-80%）
- 比值 > 1.1 → 保底（概率 90%+）

#### 5.12.2 线差法（辅助）

**原理**：用考生分数与批次线的差值，对比目标学校历年录取平均分与批次线的差值。

**公式**：

$$\Delta_{考生} = S_{考生} - L_{批次}, \quad \Delta_{目标} = \bar{S}_{目标} - L_{批次}$$

**局限**：赋分制下分数跨年不可比，线差法精度低于位次法，仅作辅助。

---

### 5.13 关键原则（策略层面）

1. **位次优先于分数**：跨年比较只用位次
2. **梯度必须合理**：冲稳保垫比例 24:40:26:6，位次梯度 10-15%
3. **专业优先于学校**（中低分段）：山东"专业+学校"模式下，专业决定就业方向
4. **风险可量化**：每个志愿有录取概率，整体有滑档概率
5. **防错清单必执行**：正式填报前逐项核查
6. **数据驱动决策**：所有判断基于 `processed/` 标准数据，不凭感觉
7. **反偏差审查**：8 类行为经济学偏差清单强制核查
8. **动态贝叶斯更新**：出现新证据必须更新概率
9. **效用个性化**：用多维效用函数匹配考生偏好
10. **博弈论视角**：在零和博弈中寻找价值洼地
11. **System2 强制激活**：8 个触发条件之一即必须深度分析
12. **多 Agent 制衡**：方案必须经 8 个专业 Agent 评审
13. **策略优化器硬约束**：正式 96 志愿候选池必须经 `algorithms/strategy_optimizer.py` 或等价逻辑做数据质量、选科证据、概率、效用、冲稳保垫配额检查
14. **价值捕获排序**：低分上好大学/好专业必须体现为 `value_capture_score`，并同时给出机会理由和风险反证
15. **双件图文交付**：正式输出必须含 Excel 志愿表和 Markdown 图文报告，裸表不得视为完整方案
16. **冲刺受控可搏**：冲刺比例和概率下限由风险档控制；深度冲只允许作为上行机会，不得挤占保/垫安全功能
17. **地区索引优先**：省份/城市偏好必须先查 `school_region_index.sqlite`，不能靠院校名关键词筛选

---

### 5.14 多 Agent 协作框架（Multi-Agent Decision System）

> **核心思想**：单个 LLM 存在认知盲区和系统性偏差。引入**多 Agent 辩论式制衡**，模拟投行投委会/医学 MDT 会诊，通过角色分工与红蓝对抗提升决策质量。
> **参考**：Anthropic Constitutional AI、Delphi Method、Red Teaming、Ensemble Learning

#### 5.14.1 8 个专业 Agent 角色

| # | Agent 名 | 角色定位 | 核心职责 | 评估视角 |
|---|---------|---------|---------|---------|
| **A1** | 数据分析师 | Quant Data Analyst | 位次映射、概率计算、蒙特卡洛 | 数据准确性 |
| **A2** | 风险审计师 | Risk Auditor | 滑档/退档/Tail Risk 量化 | 风险底线 |
| **A3** | 效用规划师 | Utility Planner | 8 维效用函数、个性化匹配 | 考生偏好 |
| **A4** | 博弈论师 | Game Theorist | 纳什均衡、价值洼地、对手分析 | 竞争策略 |
| **A5** | 偏差稽查员 | Bias Inspector | 8 类行为经济学偏差核查 | 反偏差 |
| **A6** | 政策法务师 | Policy Counsel | 政策合规、体检受限、招生章程 | 合规性 |
| **A7** | 红军对抗师 | Red Teamer | **故意挑刺**，模拟最坏情况 | 负向论证 |
| **A8** | 总统决策师 | Chief Decision Officer | 综合仲裁、最终决策 | 全局最优 |

#### 5.14.2 Agent 协作流程（MDP 七阶段）

```
Phase 1: 信息汇集（A1 主导）
  ↓ 输出：考生画像 + 候选池
Phase 2: 量化分析（A1 + A2 + A3）
  ↓ 输出：概率分布 + 效用得分 + 风险报告
Phase 3: 博弈分析（A4 主导）
  ↓ 输出：价值洼地 + 竞争策略
Phase 4: 合规审查（A6 主导）
  ↓ 输出：合规清单 + 受限项
Phase 5: 红蓝对抗（A7 vs A1-A4）
  ↓ 输出：漏洞清单 + 修正建议
Phase 6: 偏差核查（A5 主导）
  ↓ 输出：偏差指数 + 纠偏建议
Phase 7: 总裁决（A8 主导）
     输出：最终 96 志愿方案 + 决策报告
```

#### 5.14.3 每个 Agent 的工作 SOP

**A1 数据分析师**：
- 输入：考生信息 + `processed/` 数据
- 执行：位次映射、RSI 计算、Logistic 概率估计、蒙特卡洛 10000 次
- 输出：`analysis_data.json`（概率分布 + 位次稳定性）

**A2 风险审计师**：
- 输入：A1 输出 + 候选方案
- 执行：滑档概率、退档风险（5 类加权）、VaR/CVaR、Tail Risk
- 输出：`risk_audit.json`（风险等级 + 极端情况分析）

**A3 效用规划师**：
- 输入：考生 8 维偏好问卷 + 候选志愿
- 执行：AHP 权重计算、多维效用打分、HARA 风险厌恶参数
- 输出：`utility_scores.json`（每个志愿的效用分）

**A4 博弈论师**：
- 输入：A1 输出 + 历史位次波动数据
- 执行：价值洼地识别、纳什均衡分析、混合策略推荐
- 输出：`game_analysis.json`（价值洼地清单 + 策略建议）

**A5 偏差稽查员**：
- 输入：候选方案 + 考生访谈记录
- 执行：8 类偏差逐项核查、偏差指数计算
- 输出：`bias_check.json`（偏差清单 + 纠偏建议）

**A6 政策法务师**：
- 输入：2026 政策文件 + 招生章程 + 体检结论 + 选科索引查询结果
- 执行：选科硬闸门审核、体检受限检查、单科要求检查、特殊培养模式合规
- 输出：`compliance.json`（合规清单 + 受限项 + 每条志愿的 subject_check_status/evidence_id/source_file）

**A7 红军对抗师**（关键制衡）：
- 输入：A1-A6 的所有输出
- 执行：**故意寻找漏洞**，模拟 5 种最坏情况：
  1. 2026 招生计划突变（某校缩招 50%）
  2. 同分段考生扎堆（某校位次暴涨 20%）
  3. 政策临场调整（如新增选科要求或选科索引 `REVIEW` 未复核）
  4. 体检结论误判（漏检色盲）
  5. 系统性偏差（全方案锚定 2025 数据）
- 输出：`red_team.json`（漏洞清单 + 压力测试结果）

**A8 总统决策师**：
- 输入：A1-A7 全部输出
- 执行：综合仲裁、冲突解决、最终排序
- 输出：`final_decision.json`（96 志愿最终方案 + 决策报告）

#### 5.14.4 红蓝对抗机制（核心制衡）

**蓝军**（A1-A4, A6）：建设性，构建方案
**红军**（A7）：破坏性，挑战方案

**对抗规则**：
1. 红军必须提出至少 3 个"致命漏洞"
2. 蓝军必须逐条回应（接受/反驳/修正）
3. 若蓝军无法反驳，必须修改方案
4. 总统决策师（A8）仲裁争议

**红军标准提问**：
- "如果 2026 年该校缩招 30%，方案会怎样？"
- "如果考生体检漏检色弱，哪些志愿会退档？"
- "如果同分段考生扎堆冲同几个学校，滑档概率多高？"
- "这个方案是否过度依赖 2025 数据？2024/2023 数据支持吗？"
- "考生家长真的能接受这个专业的就业前景吗？"

#### 5.14.5 Agent 间通信协议

Agent 间通过结构化 JSON 通信，存于 `students/<姓名>/agent_workspace/`：

```
students/<姓名>/agent_workspace/
├── A1_data_analysis.json       # 数据分析输出
├── A2_risk_audit.json          # 风险审计输出
├── A3_utility_scores.json      # 效用打分输出
├── A4_game_analysis.json       # 博弈分析输出
├── A5_bias_check.json          # 偏差核查输出
├── A6_compliance.json          # 合规审查输出
├── A7_red_team.json            # 红军对抗输出
├── A8_final_decision.json      # 总裁决输出
├── debate_log.md               # 辩论记录
└── consensus_report.md         # 共识报告
```

#### 5.14.6 共识决策规则

**强共识**（所有 Agent 一致）→ 直接采纳
**弱共识**（≥6 个 Agent 同意）→ A8 裁决后采纳
**分歧**（≥3 个 Agent 反对）→ 必须重做 Phase 1-7
**红军否决**（A7 提出致命漏洞未解决）→ 强制重做

#### 5.14.7 LLM 执行多 Agent 的实现方式

LLM 在单次会话中**串行扮演**各 Agent（通过 prompt 角色切换）：

```
[当前角色: A1 数据分析师]
"我是 A1，专注数据分析。让我计算位次映射和概率分布..."
[输出 A1_data_analysis.json]

[当前角色: A2 风险审计师]
"我是 A2，看到 A1 的分析。让我审计风险..."
[输出 A2_risk_audit.json]

... 依次到 A8
```

或使用 Agent 工具**并行派发**子 Agent（推荐，隔离上下文）：
- 每个 Agent 派发为独立 subagent
- 各自输出 JSON 文件
- 主 Agent 汇总并裁决

#### 5.14.8 多 Agent 质量指标

| 指标 | 定义 | 目标值 |
|------|------|--------|
| Agent 共识度 | 同意 Agent 数 / 8 | ≥ 0.75 |
| 红军漏洞解决率 | 已解决漏洞 / 总漏洞 | = 1.0 |
| 偏差指数 | 触发偏差数 / 8 | ≤ 0.25 |
| 决策可解释性 | 每个志愿有 W.A.I.T.E.R 记录 | = 100% |
| 数据追溯率 | 有数据依据的判断 / 总判断 | = 100% |

#### 5.14.9 多 Agent 失败回退

若多 Agent 流程失败（如 Agent 间死锁、无法共识）：
1. **回退到 System2 单 Agent**（带 W.A.I.T.E.R 清单）
2. **强制人工审核**：标记为"需人工复核"
3. **降级输出**：仅输出数据分析和风险报告，不输出最终方案
4. **记录日志**：`log.md` 追加失败原因

#### 5.14.10 多 Agent 何时触发

**强制触发**（必须多 Agent）：
- 考生位次 < 5,000（高分段，决策成本高）
- 考生位次在本科线临界（一段线 ±5,000）
- 涉及公费生/强基/专项等特殊招生
- 考生体检受限
- 上一轮方案出现分歧或被驳回

**可选触发**（建议多 Agent）：
- 中分段常规咨询
- 首次咨询的学生

**不触发**（单 Agent 即可）：
- 简单数据查询
- 已有方案的微调

---

### 5.15 策略优化器（Strategy Optimizer）硬要求

`algorithms/strategy_optimizer.py` 是正式候选池排序与组合风险检查的基础实现。它不替代专家判断，但提供必须通过的机器闸门。

#### 5.15.1 输入字段

候选志愿进入优化器前必须至少具备：

| 字段 | 要求 |
|---|---|
| `probability` / `admission_probability` | 录取概率，0-1 或 0-100 |
| `utility` / `utility_score` | 个性化效用分，0-1 或 0-100 |
| `rsi` / `rank_stability` | 位次稳定性，默认 0.75 |
| `source_quality` / `quality_level` | 必须为 S/A/B |
| `source_file` | 必须存在，用于位次/概率/计划来源追溯 |
| `subject_check_status` | 必须为 `PASS` |
| `evidence_id` | 必须存在，用于选科证据追溯 |

价值捕获推荐字段（缺失时按保守低值处理，不得由 LLM 编造）：

| 字段 | 要求 |
|---|---|
| `school_tier` / `school_value` | 院校层次或院校价值，支持 985/211/双一流/省重点/公办/民办/职业本科等 |
| `major_value` / `major_quality` | 专业价值，0-1 或 0-100 |
| `preference_fit` / `major_preference` | 与考生目标匹配度，0-1 或 0-100 |
| `location_fit` / `region_fit` | 地域匹配度，0-1 或 0-100 |
| `value_opportunity` / `rank_value_gap` | 低位次高价值机会，必须由同层次位次对比或计划变化支持 |
| `affordability` / `cost_fit` | 学费和生活成本适配度 |
| `plan_stability` / `plan_confidence` | 招生计划稳定性 |

#### 5.15.2 阻断规则

以下任一条件触发，候选志愿不得进入正式排序：

- 缺少录取概率或概率超出合法范围
- 缺少效用分或效用分超出合法范围
- `source_quality` 非 S/A/B
- 缺少 `source_file`
- `subject_check_status` 非 `PASS`
- 缺少 `evidence_id`
- 院校代码+专业代码重复，或院校名称+专业名称重复
- 录取概率低于当前风险档 `min_rush_probability` 并被归为 `弃`
- `probability < deep_rush_probability` 但 `value_capture_score < min_deep_rush_value_capture`
- 深度冲数量超过当前风险档 `max_deep_rush`

#### 5.15.3 风险偏好与配额

优化器支持 `conservative`、`standard`、`aggressive`、`opportunistic` 四种风险偏好；默认 `standard` 近似 24:40:26:6。无论风险偏好如何，垫底志愿不足或保守滑档概率超限都会导致 `hard_gate_passed=false`。

| 风险档 | 96 志愿目标配额 | 最低冲刺概率 | 深度冲阈值 | 深度冲上限 | 保守滑档上限 |
|---|---|---:|---:|---:|---:|
| `conservative` | 14:38:34:10 | 20% | 20% | 0 | 1% |
| `standard` | 24:40:26:6 | 15% | 20% | 4 | 3% |
| `aggressive` | 30:38:22:6 | 12% | 20% | 8 | 5% |
| `opportunistic` | 34:36:20:6 | 10% | 20% | 10 | 6% |

`opportunistic` 不是默认档，只能用于考生明确接受更高上行/波动，且保/垫志愿充足、家庭对结果波动有心理准备的场景。它仍然必须通过选科、来源、证据、保守滑档和深度冲价值证据闸门。

保守滑档概率不假设 96 个志愿独立，而是用最强安全志愿的失败概率作为下限闸门：

```text
conservative_slip_probability = 1 - max(selected.probability)
```

默认上限：保守型 ≤ 1%，标准型 ≤ 3%，激进型 ≤ 5%，机会冲刺型 ≤ 6%。

#### 5.15.4 输出要求

正式交付前必须保存优化结果 JSON，至少包含：

- `hard_gate_passed`
- `gradient_counts`
- `violations`
- `independent_model_slip_probability`
- `conservative_slip_probability`
- `rush_counts`
- `warnings`
- `rush_tier`
- `value_capture_score`
- `value_capture_reasons`
- `selected`
- `blocked`

若 `hard_gate_passed=false`，不得交付为正式方案。

当前算法版本标识必须为 `strategy_optimizer_v4_rush_guard_fail_closed` 或更高；若版本低于该值，正式方案必须重跑优化器。

---

## 第六章 · 学生工作区（students/）

`students/` 存放具体学生的咨询材料，**与知识库严格隔离**。

### 6.1 隔离原则

1. **不入库**：学生材料不复制到 `raw/`、`processed/`、`wiki/`
2. **不写入 wiki**：学生姓名、成绩、志愿方案等不写入任何 wiki 页面
3. **不参与 lint**：本目录不被健康检查扫描
4. **可被 LLM 读取**：咨询时 LLM 可读 `students/<学生名>/` 下文件
5. **学生间互不污染**：每个学生独立目录

### 6.2 目录结构

```
students/
├── README.md              # 说明
└── <学生姓名>/
    ├── 基本信息.md        # 个人信息、成绩、选科、体检、偏好（含效用权重）
    ├── 操作记录.md        # 咨询过程记录、待办
    ├── 偏好问卷.md        # 8维效用权重采集
    ├── 风险评估.md        # 蒙特卡洛+偏差核查报告
    ├── system2_records.md # 【新增】System2 W.A.I.T.E.R 思考记录
    ├── subject_audit.json # 【新增】选科硬闸门审核报告
    ├── 学生材料/          # 学生提供的原始材料
    ├── agent_workspace/   # 【新增】多 Agent 协作工作区
    │   ├── A1_data_analysis.json
    │   ├── A2_risk_audit.json
    │   ├── A3_utility_scores.json
    │   ├── A4_game_analysis.json
    │   ├── A5_bias_check.json
    │   ├── A6_compliance.json
    │   ├── A7_red_team.json
    │   ├── A8_final_decision.json
    │   ├── debate_log.md
    │   └── consensus_report.md
    └── 志愿方案/          # 生成的 96 志愿方案
        ├── 2026-06-23_v1.xlsx       # Excel 版志愿表（必交付）
        ├── 2026-06-23_v1.md         # Markdown 图文说明报告（必交付）
        ├── report_assets/           # 图表、预览图、报告图片
        ├── 2026-06-23_v1.docx       # Word 版方案（可选）
        ├── 2026-06-23_v1_risk.json  # 风险报告
        ├── 2026-06-23_v1_subject_audit.json # 选科审核报告（不得含 BLOCK/REVIEW）
        ├── 2026-06-23_v1_meta.json  # 元数据（概率、效用、梯度）
        └── 2026-06-23_v1_agent.json # 【新增】多 Agent 决策报告
```

### 6.3 操作流程

#### 新增学生

1. 建 `students/<学生姓名>/`
2. 参考 `students/张三/` 模板，创建 `基本信息.md` 和 `操作记录.md`
3. 让考生填写 `偏好问卷.md`（8 维效用权重）
4. 逐项填入学生信息
5. 在 `操作记录.md` 追加咨询过程

#### 咨询时（SOP 化，9 阶段）

1. LLM 读 `基本信息.md` + `偏好问卷.md`
2. **System2 激活判定**：检查 8 个触发条件，写入 `system2_records.md`
3. **多 Agent 触发判定**：检查是否强制/可选/不触发多 Agent
4. Phase 1 信息汇集（A1）
5. Phase 2-4 量化分析 + 博弈 + 合规（A1-A4, A6）
6. Phase 5 红蓝对抗（A7 vs A1-A4, A6）
7. Phase 6 偏差核查（A5）
8. **选科硬闸门**：对 96 志愿表运行 `scripts/audit_volunteer_subjects.py`，生成 `subject_audit.json`；存在 `BLOCK`/`REVIEW` 必须回退修改，不得进入交付
9. Phase 7 总裁决（A8），生成 `final_decision.json`
10. 输出方案到 `志愿方案/`（必须含 xlsx + md + risk.json + subject_audit.json + meta.json + agent.json；docx 可选）
11. 操作记录追加到 `操作记录.md`

### 6.4 正式交付物标准（Excel + Markdown 双件）

#### 6.4.1 Excel 志愿表

正式 Excel 至少包含以下工作表：

| 工作表 | 内容 |
|---|---|
| `说明` | 考生信息、数据版本、是否通过硬闸门、使用限制 |
| `志愿表` | 96 条志愿明细，含位次、概率、效用、价值捕获、选科证据 |
| `梯度与风险` | 冲稳保垫数量、滑档概率、最强保底、Tail Risk |
| `选科审核` | `scripts/audit_volunteer_subjects.py` 审核摘要，不得含 `BLOCK/REVIEW` |
| `证据索引` | 每条志愿的 `source_file`、`evidence_id`、质量等级 |

`志愿表` 必填列：

```text
志愿序号, 梯度, 院校代码, 院校名称, 专业代码, 专业名称,
地区/城市, 2026计划数, 近年最低位次, 加权参考位次, RSI,
模拟录取概率, 效用分, value_capture_score, value_capture_reasons,
低分高价值解释, 主要风险, 选科审核状态, 选科要求,
选科证据ID, 地区审核状态, 地区证据ID, 数据来源, 数据质量, 备注
```

#### 6.4.2 Markdown 图文说明报告

正式 Markdown 报告必须专业、可读、可审计，至少包含：

1. **封面摘要**：考生位次、选科、批次、目标、结论状态（通过/未通过）
2. **数据依据**：一分一段、投档表、招生计划、选科 SQLite 索引、院校地区 SQLite 索引、互联网来源准入结果
3. **定位图表**：位次区间、同位次等效分、目标梯度分布
4. **志愿梯度图**：冲/稳/保/垫数量条形图或 Mermaid 图
5. **低分高价值机会图**：高 `value_capture_score` 志愿清单 + 机会原因 + 反证风险
6. **选科审核图表**：PASS/BLOCK/REVIEW 汇总，正式报告必须全部 PASS
7. **风险面板**：滑档概率、保守滑档、Tail Risk、最弱保底、退档风险
8. **System2 W.A.I.T.E.R 记录**：关键取舍和反向论证
9. **8 Agent 审核摘要**：A1-A8 结论、A7 红队漏洞和修正记录
10. **最终交付清单**：Excel、JSON、审核报告、图表资产路径

报告必须使用 Markdown 表格、Mermaid 图或本地图片资产形成“图文并茂”的阅读体验；不得只输出 96 行裸表。若方案未通过硬闸门，报告标题和摘要必须明确写“未通过硬闸门，不可正式填报”。

#### 隐私保护

- 身份证号、考生号、电话等敏感信息只存于 `students/`
- 志愿方案中的学校专业可引用 `processed/` 数据，但学生身份不写入 wiki
- 多个学生之间互不干扰

---

## 第七章 · 页面约定

### 7.1 YAML frontmatter

每个 wiki 页面顶部必须有 frontmatter：

```yaml
---
title: 页面标题
tags: [标签1, 标签2]
created: YYYY-MM-DD
updated: YYYY-MM-DD
sources: ["raw/路径/文件名", "processed/路径/文件名"]
---
```

### 7.2 四类页面职责

| 类型 | 前缀 | 路径 | 职责 |
|---|---|---|---|
| 概念页 | `concept_` | `wiki/concepts/` | 单一概念的权威定义 |
| 实体页 | `entity_` | `wiki/entities/` | 具体对象 |
| 主题页 | `topic_` | `wiki/topics/` | 跨概念综合分析 |
| 来源页 | `source_` | `wiki/sources/` | 数据来源说明 |

**特殊页面**：`index.md`、`log.md`、`overview.md`、`lint_*.md` 在 wiki 根目录。

### 7.3 命名约定

- 页面文件名：`<type>_<中文名>.md`
- 标签：简短名词，多个用逗号分隔
- `[[页面名]]` 链接使用文件名（不含 `.md`）

### 7.4 交叉引用

- 每个页面**至少包含 1 个出链**（避免死页）
- 新建页面时，在相关页面添加反向链接
- 链接目标必须存在，`<year>` 等占位符除外

---

## 第八章 · 处理脚本

`scripts/` 下的脚本可重复运行，输出幂等：

| 脚本 | 输入 | 输出 |
|---|---|---|
| `_process_score_table.py` | `raw/<year>/一分一段表.xls` | `processed/一分一段表/<year>.csv\|json` |
| `_process_admission.py` | `raw/<year>/常规批第N次投档表/` | `processed/投档表/<year>_round<n>.csv\|json` |
| `_process_scorelines.py` | `raw/<year>/分数线/*.pdf` | `processed/分数线/<year>.json` |
| `_process_plans.py` | `raw/<year>/*志愿计划/` | `processed/志愿计划/<year>.csv\|json` |
| `_process_subject_requirements.py` | `raw/2026/政策文件原件/*选考科目要求*.pdf` | `processed/选科要求/*.csv\|json` |
| `build_subject_index.py` | `processed/选科要求/2024版本科.json` 等 4 个官方派生 JSON | `processed/选科要求/subject_index.sqlite` + `subject_index_meta.json` |
| `check_subject_eligibility.py` | 年份/层次/三科 + 院校代码/专业代码 | 单条选科可报性 JSON（PASS/BLOCK/REVIEW + evidence_id） |
| `audit_volunteer_subjects.py` | CSV/JSON/XLSX 志愿表 + 年份/层次/三科 | 选科硬闸门审核报告 JSON |
| `test_subject_index.py` | `subject_index.sqlite` | 索引行数、单条查询、整表闸门 smoke test |
| `build_school_region_index.py` | `processed/选科要求/*.json` 的院校与 province 字段 | `processed/院校地区/school_region_index.sqlite` + meta |
| `check_school_region.py` | 地区偏好 + 院校名称/选科院校代码 | MATCH/NO_MATCH/REVIEW JSON |
| `test_school_region.py` | `school_region_index.sqlite` | 省份非名称匹配、苏州别名、多校区 REVIEW smoke test |
| `internet_source_policy.py` | URL 或 URL 列表 | 互联网来源质量上限与正式使用准入报告 |
| `test_strategy_optimizer.py` | 内置模拟候选池 | 策略优化器 smoke test |

`algorithms/` 下的策略脚本：

| 脚本 | 输入 | 输出 |
|---|---|---|
| `strategy_optimizer.py` | 候选志愿 CSV/JSON（概率、效用、质量等级、选科证据、价值捕获字段） | 排序后的 96 志愿组合、blocked 清单、梯度统计、滑档基线、`value_capture_score` |

新增数据类型时，复制现有脚本模式，保持字段命名一致。

---

## 第九章 · 扩展指引

### 9.1 添加新年份

1. 建 `raw/<year>/`，按目录结构组织
2. 运行处理脚本生成 `processed/`
3. 建实体页 `wiki/entities/entity_<year>年高考.md`
4. 更新 `wiki/index.md` 和 `wiki/log.md`

### 9.2 添加新概念

1. 建 `wiki/concepts/concept_<名称>.md`
2. 在 `wiki/index.md` 概念分类下添加链接
3. 在相关页面添加 `[[concept_<名称>]]` 交叉引用
4. 追加 `log.md`

### 9.3 添加新主题

1. 建 `wiki/topics/topic_<名称>.md`
2. 在 `wiki/index.md` 主题分类下添加链接
3. 追加 `log.md`

### 9.4 添加新数据集

1. 在 `raw/` 收集原始资料
2. 编写 `scripts/_process_<名称>.py`
3. 在 `processed/<名称>/` 输出 CSV+JSON
4. 写 `processed/<名称>/_meta.json` 元数据
5. 更新 `processed/README.md` 和 `wiki/sources/source_标准数据字典.md`
6. 追加 `log.md`

### 9.5 添加新算法

1. 编写 `algorithms/<算法名>.py`（含单元测试）
2. 建 `wiki/concepts/concept_<算法名>.md` 说明数学原理
3. 在 `wiki/topics/topic_高级决策模型.md` 添加集成说明
4. 更新 `algorithms/README.md`
5. 追加 `log.md`

---

## 第十章 · 版本与变更

| 版本 | 日期 | 变更 |
|------|------|------|
| v1.0 | 2026-06-22 | 初版 schema |
| v2.0 | 2026-06-23 | 加固版：引入精算/量化金融/运筹学/博弈论/ML 级算法，12 条铁律，8 类偏差核查，蒙特卡洛+贝叶斯+凯利+投资组合理论 |
| v3.0 | 2026-06-23 | 多Agent加固版：System2 严格映射（W.A.I.T.E.R 清单+8 触发条件）+ 8 Agent 协作框架（A1-A8 辩论式制衡，红蓝对抗，共识阈值），铁律升级至 16 条 |
| v3.1 | 2026-06-23 | 官方数据源加固版：山东省教育招生考试院（sdzk.cn）为唯一正式主源，非官方数据降为线索，processed 元数据必须记录来源等级和核验状态 |
| v3.2 | 2026-06-23 | 选科防幻觉索引版：新增 Z17 选科硬闸门；选科判断必须走 SQLite 索引、evidence_id 追溯与 96 志愿 `BLOCK`/`REVIEW` 阻断 |
| v3.3 | 2026-06-23 | 互联网数据准入与策略优化版：新增 Z18；互联网来源先定级，策略优化器强制拦截非正式来源、选科未 PASS、无 evidence_id 或配额不合格候选 |
| v3.4 | 2026-06-23 | 策略算法失败关闭版：新增 Z19；异常概率、缺效用/来源、重复志愿、保守滑档超限一律阻断，输出 `strategy_optimizer_v2_fail_closed` 版本标识 |
| v3.5 | 2026-06-23 | 价值捕获与图文双交付版：新增 Z20/Z21；低分高价值机会纳入 `value_capture_score`，正式方案必须输出 Excel 志愿表 + Markdown 图文报告 |
| v3.6 | 2026-06-23 | 受控冲刺概率加固版：新增 Z22 与 Rush Guard；新增 `opportunistic` 风险档，深度冲必须满足概率下限、价值证据、数量上限和保/垫闸门 |
| v3.7 | 2026-06-24 | 院校地区索引加固版：新增 Z23；省份查询走 `province` 字段，城市/多校区走 MATCH/NO_MATCH/REVIEW，禁止 `school_name contains 省名` |

---

> **本文件是知识库的宪法。任何操作必须遵循本文件。**
> 决策不是凭感觉，是凭算法；风险不是模糊词，是可量化指标；志愿不是赌注，是优化解。
